# Co-op RPG (Radmin + WebSocket)

Кооперативная 2D-RPG на чистом JavaScript + Node.js. Хост поднимает сервер, друг подключается через Radmin VPN. Никаких облаков, никаких npm-фреймворков на клиенте, никакой сборки.

---

## 1. Что это

- **Клиент** — статика: `index.html` + ES-модули без бандлера, рендер на `<canvas>` 2D.
- **Сервер** — один файл `server.js`: раздаёт статику по HTTP и держит WebSocket для синхронизации игроков.
- **Связь** — `ws://<ip>:3000`, JSON-сообщения.
- **Сеть** — Radmin VPN даёт обоим игрокам виртуальный LAN. Хост открывает свой Radmin-IP, друг стучится туда.

Проект **намеренно минималистичный**. Не притаскивать сюда фреймворки, сборщики, TypeScript, React, Phaser и т.п. без явного запроса.

---

## 2. Структура репозитория

```
.
├── package.json          # один скрипт start, одна зависимость ws
├── server.js             # HTTP-статика + WebSocket, всё в одном файле
├── public/
│   ├── index.html        # единственная HTML-страница
│   ├── style.css         # всё оформление меню и канваса
│   └── js/
│       ├── main.js       # точка входа: меню, игровой цикл, рендер
│       ├── input.js      # клавиатура
│       ├── camera.js     # следит за игроком, перевод мир↔экран
│       ├── world.js      # детерминированная генерация карты
│       ├── player.js     # движение + коллизии
│       └── net.js        # WebSocket-клиент
└── README.md
```

**Правило:** клиентский код живёт **только** в `public/`. Серверный — **только** в корне (`server.js`). Не смешивать.

---

## 3. Как запустить

### Хост
```bash
npm install
npm start
```
В консоли появится:
```
=== Server running ===
  local:  http://localhost:3000
  LAN:    http://192.168.x.x:3000
  LAN:    http://26.x.x.x:3000 ← Radmin (этот дай другу)
```
Открыть `http://localhost:3000` → имя → поле IP **оставить пустым** → «Подключиться».

### Друг
Открыть `http://26.x.x.x:3000` (Radmin-IP хоста) → имя → «Подключиться» (поле заполнено автоматически).

### Требования
- Node.js 18+
- Radmin VPN (или любая LAN), оба в одной сети
- Node.js разрешён в брандмауэре Windows для **частных сетей**

---

## 4. Сетевой протокол

Единственный транспорт — WebSocket, формат — JSON. Все типы сообщений перечислены ниже. **При добавлении новых типов — обновлять этот раздел.**

### Клиент → сервер

| `type`  | Поля        | Когда                              |
|---------|-------------|------------------------------------|
| `join`  | `name`      | сразу после открытия WS            |
| `state` | `x`, `y`    | 20 раз в секунду (каждые 50 мс)    |

### Сервер → клиент

| `type`   | Поля                              | Когда                                      |
|----------|-----------------------------------|--------------------------------------------|
| `init`   | `id`, `players[]`                 | в ответ на `join`, только отправителю      |
| `join`   | `player`                          | всем остальным, когда кто-то вошёл         |
| `leave`  | `id`                              | всем, когда кто-то отключился              |
| `state`  | `players[]`                       | broadcast 20 раз/с всем клиентам           |

### Формат игрока
```json
{ "id": 3, "name": "Вася", "x": 128, "y": 96, "hue": 41 }
```
- `id` — серверный, монотонно растёт с 1.
- `hue` — `(id * 137) % 360`, задаёт цвет кружка.
- `name` — обрезается до 12 символов на сервере.

### Правила протокола
1. **Сервер — источник правды** для `id`, `name`, `hue` и позиций в рассылке.
2. **Клиент двигает себя сам** (client-authoritative movement). Сервер не пересчитывает физику — только принимает координаты.
3. **Интерполяция на клиенте:** чужие игроки догоняют целевую позицию через `x += (tx - x) * 0.25`. Не менять на телепорт — это убьёт плавность.
4. **Не спамить.** 20 Гц — осознанный выбор. Чаще не надо, реже — рывки.
5. **Никаких бинарных форматов** без явной необходимости. JSON читаем и отлаживается в DevTools.

---

## 5. Архитектурные правила

### 5.1 Клиент
- **Никаких зависимостей.** Только ES-модули и встроенные API браузера.
- **Никакого бандлера.** `<script type="module">` работает как есть.
- **Никакого TypeScript.** Чистый JS.
- **Каждый модуль — один класс или одна функция**, экспортируется через `export`. Никаких `window.*`, никаких глобалов.
- **main.js — единственное место**, где создаются экземпляры и склеиваются модули. Остальные модули друг о друге не знают.

### 5.2 Сервер
- **Один файл.** Если `server.js` перевалит за ~200 строк — можно разбить на `server/` с модулями, но не раньше.
- **Никакого фреймворка** (Express, Fastify, Koa). Только `http` из stdlib.
- **Никакой БД.** Состояние игроков живёт в памяти. Упал сервер — мир пересоздался.
- **Логирование** — простой `console.log` с префиксами `+`/`-`.

### 5.3 Мир и генерация
- **Мир детерминирован.** Сид `12345` зашит в `world.js`. Оба клиента строят **одинаковую** карту.
- **Мир не синхронизируется.** Никаких изменений карты пока нет. Если появятся (руда, постройки) — они уходят на сервер, не генерируются на клиентах.
- **Тайл 32×32**, карта 60×45 тайлов. Менять только вместе с серверными `SPAWNS`.

---

## 6. Соглашения по коду

### Имена
- Файлы модулей — `lowercase.js` (`net.js`, `player.js`).
- Классы — `PascalCase` (`Player`, `Camera`, `Net`).
- Методы и переменные — `camelCase` (`sendState`, `isBlocked`).
- Приватные методы — префикс `_` (`_handle`, `_onData`).
- Константы — `UPPER_SNAKE` (`PORT`, `TILE`, `SPAWNS`, `SEED`).

### Стиль
- 2 пробела, без табов.
- Точка с запятой — **есть**.
- Одинарные кавычки в JS.
- Фигурные скобки — K&R (открывающая на той же строке).
- Стрелочные функции для колбэков, обычные `function` для именованных.
- Не сокращать имена: `player`, не `pl`; `connection`, не `c` (кроме коротких циклов `for (const c of conns)` — допустимо).

### Комментарии
- **Русский язык.** Это осознанно — вся документация и переписка на русском.
- Секции в файлах разделяются `// ---------- название ----------`.
- Не комментировать очевидное (`// x = 5` — плохо). Комментировать **почему**, а не **что**.

### Порядок в файле
1. Импорты
2. Константы
3. Класс / главная функция
4. Экспорт (обычно `export class` в начале)

---

## 7. Что НЕЛЬЗЯ делать

Это самые важные правила. Нарушение ломает проект или выкидывает нас из архитектуры.

### Категорически нельзя
1. **Добавлять клиентские зависимости** (React, Vue, Phaser, Three.js, lodash, axios…). Проект построен на голом JS.
2. **Ставить сборщик** (Webpack, Vite, esbuild, Rollup). Мы запускаем файлы как есть.
3. **Писать на TypeScript** и компилировать.
4. **Заменять WebSocket на HTTP-поллинг.** `setInterval` + `fetch` убьёт производительность и смысл проекта.
5. **Заменять WS на Socket.IO.** Это тащит свою библиотеку и клиентскую, и серверную.
6. **Менять порт 3000** без обновления `public/js/main.js` (там зашит `ws://ip:3000`).
7. **Делать сервер авторитетным для движения.** Сейчас движение client-side — это фича, не баг. Если переделывать — только по явному запросу.
8. **Коммитить `node_modules/`.** Есть `.gitignore` — не трогать.
9. **Менять `SEED` в `world.js`** без синхронного изменения на сервере (если мир когда-нибудь уйдёт на сервер).
10. **Писать имена и сообщения на английском.** Весь UI, комментарии и логи — на русском.

### Нежелательно
- Раздувать `main.js` больше ~200 строк. Разбивать на модули.
- Делать классы с публичными полями вместо методов.
- Использовать `var`. Только `let`/`const`.
- Создавать глобальные переменные на `window`.
- Добавлять `console.log` в горячий путь (игровой цикл, `_handle` каждого сообщения).

---

## 8. Как расширять проект

Порядок сложности, согласованный с владельцем:

1. **Враги** — сервер спавнит мобов, шлёт позиции. Урон считает сервер.
2. **Ресурсы** — деревья/руда как объекты на сервере, ломаются кликом.
3. **Инвентарь** — у каждого игрока свой, но рецепты общие.
4. **Крафт** — UI поверх канваса.
5. **Строительство** — блоки ставятся через сервер.
6. **HP и бой** — базовая боевая система.

**При добавлении врагов:**
- Позиции мобов идут в `state`-сообщение рядом с `players`.
- Урон — отдельный тип `hit`, сервер проверяет дистанцию.
- HP мобов хранится на сервере, клиенты получают только актуальные значения.
- Не спавнить мобов на клиентах — только на сервере, иначе рассинхрон.

**При добавлении мира, который меняется:**
- Карта переезжает на сервер целиком.
- Клиент шлёт `action`, сервер валидирует и рассылает `world_update`.
- Тогда же — убрать `world.js` с клиента или превратить его в тонкий слой рендера.

---

## 9. Отладка

### Если не коннектится
1. Radmin VPN активен у обоих, видите друг друга в списке.
2. Пинг через Radmin (правый клик → «Пинг») проходит.
3. Node.js разрешён в брандмауэре Windows для **частных сетей**.
4. Порт 3000 свободен: `netstat -ano | findstr :3000`.
5. F12 → Console: если `WebSocket connection failed` — не туда стучишься или фаервол.

### Полезные команды
```bash
# сменить порт (тогда править main.js!)
PORT=3001 npm start

# посмотреть, кто слушает 3000
netstat -ano | findstr :3000
```

### DevTools
- **Network → WS** — видно каждое сообщение, можно смотреть поток в реальном времени.
- **Console** — логи `+ Вася (id=1) — всего 2` печатает **сервер**, не браузер.

---

## 10. Правила для AI, работающего с репозиторием

Если ты (AI) впервые открыл этот репозиторий — **прочитай этот раздел целиком**.

1. **Язык — русский.** Комментарии, README, UI, коммиты, ответы пользователю. Английский — только в идентификаторах кода.
2. **Никаких новых зависимостей без явного запроса.** Если решение требует npm-пакета — сначала спроси.
3. **Не менять архитектуру** (сервер/клиент, транспорт, формат сообщений) без явного запроса.
4. **Правила стиля — см. раздел 6.** Если сомневаешься — смотри на существующий код и повторяй.
5. **Протокол — см. раздел 4.** Новые типы сообщений добавляй **в обе стороны** (клиент + сервер) и **обновляй таблицу**.
6. **Не трогать `package.json`** без причины. Единственная зависимость — `ws`.
7. **Перед добавлением файла** — проверь, что он не дублирует существующий модуль.
8. **Тестируй локально:** `npm install && npm start` — сервер должен подняться без ошибок. Открой `http://localhost:3000` — меню должно появиться.
9. **Не рефакторить чужое без запроса.** Если видишь стилистическую проблему — упомяни, но не правь молча.
10. **Не писать тесты** (jest, mocha, vitest) — проект их не использует и не планирует в текущем виде.
11. **Не добавлять CI/CD** (GitHub Actions, GitLab CI) без запроса.
12. **Не менять кодировку файлов.** UTF-8 без BOM, LF-переводы строк.
13. **Сомневаешься — спроси.** Лучше уточнить, чем сломать установленный порядок.

### Красные флаги — если видишь такое в своём решении, остановись
- Ты ставишь `npm install <что-то новое>` — стоп, спроси.
- Ты добавляешь `<script src="https://cdn...">` в HTML — стоп, спроси.
- Ты создаёшь файл `webpack.config.js`, `vite.config.js`, `tsconfig.json` — стоп, это не наш стек.
- Ты пишешь `import React` — стоп.
- Ты пытаешься заменить `ws` на `socket.io` — стоп.
- Ты добавляешь `.ts`-файлы — стоп.
- Ты переписываешь `server.js` на Express — стоп.

---

## 11. Лицензия и авторство

Учебно-развлекательный проект. Никакой лицензии, никаких гарантий. Код свободен для правок в рамках правил выше.

---

## 12. Быстрая шпаргалка

```bash
# поднять
npm install
npm start

# хост:      http://localhost:3000       (поле IP пустое)
# друг:      http://<radmin-ip>:3000     (поле заполнено авто)

# порт по умолчанию: 3000
# транспорт:         WebSocket, JSON
# частота рассылки:  20 Гц
# карта:             фиксированный сид 12345, 60×45 тайлов, тайл 32px
# движение:          client-authoritative, интерполяция 0.25
```

**Помни: минимум зависимостей, максимум читаемости, всё на русском.**
```

---

**Что здесь важно:**

- **Раздел 10** — это ядро запроса: чёткий список правил для AI, включая «красные флаги», по которым легко заметить, что решение уходит не туда.
- **Раздел 4** — протокол в виде таблиц, чтобы при добавлении новых сообщений AI точно знал, куда смотреть и что обновлять.
- **Раздел 7** — «что нельзя», с явным разделением на «категорически» и «нежелательно».
- **Раздел 6** — стиль кода, чтобы не было споров про кавычки и отступы.

---

**Optimization Tip:** If you want the README to auto-track the protocol (so an AI can't forget to update it), consider moving the message tables into a separate `PROTOCOL.md` and referencing it from both `README.md` and `server.js` via a comment like `// see PROTOCOL.md`. That way the protocol has a single source of truth, and adding a message type forces a doc update in exactly one place. You can also add a short `AGENTS.md` at the repo root that just says "read README.md section 10 before doing anything" — many AI tools scan for that filename automatically.